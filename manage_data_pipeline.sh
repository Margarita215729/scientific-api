#!/bin/bash\n\n# Script to manage data pipelines for the Scientific API\n# (e.g., ensure DB/collections exist, trigger data preprocessing)\n\nset -e # Exit immediately if a command exits with a non-zero status.\n\n# --- Configuration ---\n# Load from deploy.env or set directly\nif [ -f deploy.env ]; then\n    echo \" sourcing environment variables from deploy.env\"\n    export $(grep -v \'^#\' deploy.env | xargs)\nfi\n\nAZURE_RESOURCE_GROUP=\"${AZURE_RESOURCE_GROUP:?Please set AZURE_RESOURCE_GROUP}\"\nCOSMOS_DB_ACCOUNT_NAME=\"${COSMOS_DB_ACCOUNT_NAME:?Please set COSMOS_DB_ACCOUNT_NAME}\" # Имя вашего Cosmos DB аккаунта, например, scientific-api-server\nCOSMOS_DB_DATABASE_NAME=\"${COSMOS_DATABASE_NAME:?Please set COSMOS_DATABASE_NAME}\"\nWEB_APP_NAME=\"${AZURE_APP_NAME:?Please set AZURE_APP_NAME}\" # Имя вашего Web App, например, scientific-api\n\n# Получаем URL веб-приложения\nWEB_APP_URL=\"https://$(az webapp show --resource-group \"$AZURE_RESOURCE_GROUP\" --name \"$WEB_APP_NAME\" --query \"defaultHostName\" -o tsv)\"\n\nif [ -z \"$WEB_APP_URL\" ] || [ \"$WEB_APP_URL\" == \"https\" ]; then\n    echo \"❌ Error: Could not retrieve Web App URL for \'$WEB_APP_NAME\'. Please ensure it is deployed and running.\" >&2\n    exit 1\nfi\n\n# Коллекции, которые должны существовать (и их shard keys)\n# Формат: \"collection_name:shard_key_path\"\n# Shard key path для MongoDB это имя поля, например, \"user_id\", не \"/user_id\"\ndeclare -A COLLECTIONS_TO_ENSURE=(\n    [\"users\"]=\"username\" \n    [\"api_keys\"]=\"user_id\"\n    [\"astronomical_objects\"]=\"catalog_source\"\n    [\"astronomical_data\"]=\"object_id\"\n    [\"search_history\"]=\"user_id\"\n    [\"ml_analysis_results\"]=\"user_id\"\n    [\"api_cache\"]=\"_id\" # _id часто используется как shard key для кэша\n    [\"system_statistics\"]=\"metric_name\"\n)\n\n# --- Functions ---\n\ncheck_azure_login() {\n    echo \"\n🔗 Checking Azure login status...\"\n    if ! az account show &> /dev/null; then\n        echo \"   Attempting to login to Azure...\"\n        az login --use-device-code\n    fi\n    echo \"   Azure login verified.\"\n}\n\nensure_database_exists() {\n    echo \"\n确保数据库 \'$COSMOS_DB_DATABASE_NAME\' в аккаунте \'$COSMOS_DB_ACCOUNT_NAME\' существует...\"\n    if az cosmosdb mongodb database show --account-name \"$COSMOS_DB_ACCOUNT_NAME\" --name \"$COSMOS_DB_DATABASE_NAME\" --resource-group \"$AZURE_RESOURCE_GROUP\" &> /dev/null; then\n        echo \"   ✅ База данных \'$COSMOS_DB_DATABASE_NAME\' уже существует.\"\n    else\n        echo \"   База данных \'$COSMOS_DB_DATABASE_NAME\' не найдена. Создание...\"\n        az cosmosdb mongodb database create --account-name \"$COSMOS_DB_ACCOUNT_NAME\" --name \"$COSMOS_DB_DATABASE_NAME\" --resource-group \"$AZURE_RESOURCE_GROUP\"\n        echo \"   ✅ База данных \'$COSMOS_DB_DATABASE_NAME\' создана.\"\n    fi\n}\n\nensure_collections_exist() {\n    echo \"\n确保 коллекции существуют и настроены shard keys...\"\n    for collection_name in \"${!COLLECTIONS_TO_ENSURE[@]}\"; do\n        shard_key_path=\"${COLLECTIONS_TO_ENSURE[$collection_name]}\"\n        echo \"  Проверка коллекции: \'$collection_name\' с shard key: \'$shard_key_path\'...\"\n        \n        # Проверяем, существует ли коллекция\n        if az cosmosdb mongodb collection show --account-name \"$COSMOS_DB_ACCOUNT_NAME\" --database-name \"$COSMOS_DB_DATABASE_NAME\" --name \"$collection_name\" --resource-group \"$AZURE_RESOURCE_GROUP\" &> /dev/null; then\n            echo \"    ✅ Коллекция \'$collection_name\' уже существует.\"\n            # TODO: Можно добавить проверку, соответствует ли shard key ожидаемому, но это сложнее через CLI\n        else\n            echo \"    Коллекция \'$collection_name\' не найдена. Создание с shard key \'$shard_key_path\'...\"\n            # Обратите внимание: --shard \"$shard_key_path\" может потребовать, чтобы поле существовало или было частью _id.\n            # Для Cosmos DB MongoDB API, shard key должен быть свойством документа.\n            # Если shard_key_path это \"_id\", то Cosmos DB обычно обрабатывает это автоматически.\n            # Для других полей, убедитесь, что они будут присутствовать в документах.\n            az cosmosdb mongodb collection create \\\n                --account-name \"$COSMOS_DB_ACCOUNT_NAME\" \\\n                --database-name \"$COSMOS_DB_DATABASE_NAME\" \\\n                --name \"$collection_name\" \\\n                --resource-group \"$AZURE_RESOURCE_GROUP\" \\\n                --shard \"$shard_key_path\" \\\n                --throughput 400 # Укажите throughput или используйте serverless/autoscale\n            echo \"    ✅ Коллекция \'$collection_name\' создана.\"\n        fi\n    done\n}\n\ntrigger_api_endpoint() {\n    local endpoint_path=\"$1\"\n    local description=\"$2\"\n    local method=\"${3:-POST}\" # По умолчанию POST\n    local payload_json=\"${4:-{\}}\" # По умолчанию пустой JSON\n\n    echo \"\n TRIGGERING: $description ($method $WEB_APP_URL$endpoint_path)\"\n    \n    response=$(curl -s -X \"$method\" \\\n        -H \"Content-Type: application/json\" \\\n        -d \"$payload_json\" \\\n        \"$WEB_APP_URL$endpoint_path\" \\\n        -w \"\\nHTTP_STATUS_CODE:%{http_code}\")\n    \n    http_status=$(echo \"$response\" | sed -n \'s/.*HTTP_STATUS_CODE://p\')\n    body=$(echo \"$response\" | sed \'s/HTTP_STATUS_CODE:.*//\')\n\n    echo \"   Response Body: $body\"\n    echo \"   HTTP Status: $http_status\"\n\n    if [[ \"$http_status\" -ge 200 && \"$http_status\" -lt 300 ]]; then\n        echo \"   ✅ $description успешно запущен(а).\"\n        task_id=$(echo \"$body\" | jq -r \'.task_id // .job_id // \"unknown_task_id\"\')\n        echo \"   Task ID: $task_id\"\n        if [ \"$task_id\" != \"unknown_task_id\" ] && [ \"$task_id\" != \"null\" ]; then\n            monitor_task \"$task_id\"\n        fi\n    else\n        echo \"   ❌ Ошибка при запуске $description. Статус: $http_status\"\n    fi\n}\n\nmonitor_task() {\n    local task_id=\"$1\"\n    echo \"   🔄 Мониторинг задачи \'$task_id\'... (Ctrl+C для остановки)\"\n    \n    local status=\"started\"\n    local progress=0\n    local attempts=0\n    local max_attempts=60 # Макс. попыток (например, 5 минут, если интервал 5 сек)\n\n    while [[ \"$status\" != \"completed\" && \"$status\" != \"failed\" && \"$attempts\" -lt \"$max_attempts\" ]]; do\n        sleep 10 # Интервал проверки\n        attempts=$((attempts+1))\n        \n        task_info=$(curl -s -X GET \"$WEB_APP_URL/api/heavy/task-status/$task_id\")\n        status=$(echo \"$task_info\" | jq -r \'.status // \"unknown\"\')\n        progress=$(echo \"$task_info\" | jq -r \'.progress // 0\')\n        message=$(echo \"$task_info\" | jq -r \'.message // \"-\"\')\n        \n        echo \"    [Попытка $attempts/$max_attempts] Статус задачи \'$task_id\': $status ($progress%) - $message\"\n        \n        if [[ \"$status\" == \"completed\" ]]; then\n            echo \"   ✅ Задача \'$task_id\' успешно завершена.\"\n            echo \"   Детали: $task_info\"\n            break\n        elif [[ \"$status\" == \"failed\" ]]; then\n            echo \"   ❌ Задача \'$task_id\' завершилась с ошибкой.\"\n            echo \"   Детали: $task_info\"\n            break\n        fi\n    done\n    if [[ \"$attempts\" -ge \"$max_attempts\" ]]; then\n        echo \"   ⚠️  Мониторинг задачи \'$task_id\' завершен по таймауту. Проверьте статус позже.\"\n    fi\n}\n\n# --- Main Script Logic ---\ncheck_azure_login\n\n# 1. Убедиться, что БД и коллекции существуют\n# Раскомментируйте, если хотите, чтобы скрипт управлял созданием БД/коллекций.\n# Внимание: создание коллекций с shard key может потребовать, чтобы аккаунт Cosmos DB\n# был уже настроен с нужными параметрами или использовался serverless/autoscale.\n# echo \"--- Шаг 1: Проверка/создание базы данных и коллекций ---\"\n# ensure_database_exists\n# ensure_collections_exist\n\necho \"\n--- Шаг 2: Запуск пайплайна предварительной обработки данных каталогов ---\"\ntrigger_api_endpoint \"/api/heavy/astro/trigger-preprocessing\" \"Полный препроцессинг каталогов\"\n\n# Пример запуска кастомной обработки (если нужно)\n# echo \"\n--- Шаг 3: Запуск кастомной обработки данных (пример: очистка) ---\"\n# CUSTOM_CONFIG_JSON=\'{\"processing_type\": \"clean\"}\' # Другие параметры можно добавить в config\n# trigger_api_endpoint \"/api/heavy/data/custom-process\" \"Кастомная очистка данных\" \"POST\" \"$CUSTOM_CONFIG_JSON\"\n\necho \"\n🎉 Скрипт управления пайплайнами завершен.\"\n 