# Notebooks

1. `01_cosmology_ingest_and_graph.ipynb`
   - SDSS DR17 + DESI DR1 ingestion
   - normalization to canonical columns
   - graph construction to match target N (1e3, 1e4)

2. `02_quantum_graphs_and_comparison.ipynb`
   - quantum graph ensembles строго по критериям (топологии, N, контролируемые W/p, ансамбли, воспроизводимость)
   - единый набор метрик + сравнение с космологическими графами

Run 01 first, then 02.

Artifacts:
- `data/` — raw/processed tables and graph datasets
- `outputs/` — figures, reports, experiment tables
