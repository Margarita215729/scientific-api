{
  "name": "scientific-api",
  "version": "1.0.0",
  "description": "Scientific API for large-scale structure data",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^28.20.0"
  }
} 